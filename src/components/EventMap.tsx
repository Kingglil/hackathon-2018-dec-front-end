import * as React from "react";

export interface EventMapProps {}

export interface EventMapState {}

class EventMap extends React.Component<EventMapProps, EventMapState> {
  constructor(props: EventMapProps) {
    super(props);
  }
  render() {
    return <div />;
  }
}

export default EventMap;
